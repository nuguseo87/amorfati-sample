{
  "name": "amorfati-site",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev .",
    "db:local": "wrangler d1 execute amorfati --local --file=./schema.sql",
    "deploy": "wrangler pages deploy ."
  },
  "devDependencies": {
    "wrangler": "^4.130.0"
  }
}
